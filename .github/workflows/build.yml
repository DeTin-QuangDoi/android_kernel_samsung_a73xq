name: Samsung A73 Vanilla Kernel Build

on:
  workflow_dispatch: 
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Kernel Source
        uses: actions/checkout@v4

      - name: Install Dependencies
        run: |
          sudo apt update
          sudo apt install -y git aria2 device-tree-compiler lz4 xz-utils zlib1g-dev \
          openjdk-17-jdk gcc g++ python3 python-is-python3 p7zip-full \
          android-sdk-libsparse-utils erofs-utils default-jdk gnupg flex bison \
          gperf build-essential zip curl libc6-dev libncurses-dev libx11-dev \
          libreadline-dev libgl1 libgl1-mesa-dev make bc grep tofrodos \
          libxml2-utils xsltproc libtinfo6 repo cpio kmod openssl libelf-dev \
          pahole libssl-dev libarchive-tools zstd rsync
          
          # Cài đặt thư viện ncurses cũ cần thiết cho linker của toolchain
          wget -q http://security.ubuntu.com/ubuntu/pool/universe/n/ncurses/libtinfo5_6.3-2ubuntu0.1_amd64.deb
          sudo dpkg -i libtinfo5_6.3-2ubuntu0.1_amd64.deb || sudo apt-get install -f -y
          rm libtinfo5_6.3-2ubuntu0.1_amd64.deb

      - name: Fetch Toolchains
        run: |
          # 1. Dọn dẹp thư mục toolchain cũ nếu có rác
          rm -rf toolchain
          mkdir -p toolchain/gcc/linux-x86/aarch64
          mkdir -p toolchain/llvm-arm-toolchain-ship/10.0
          
          # 2. Tải GCC 4.9 (Dùng phương pháp clone vào thư mục chưa tồn tại để đảm bảo cấu trúc)
          echo "Cloning GCC 4.9..."
          git clone --depth=1 https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/aarch64/aarch64-linux-android-4.9 \
          toolchain/gcc/linux-x86/aarch64/aarch64-linux-android-4.9
          
          # 3. Tải Clang r530567 từ AOSP
          echo "Downloading AOSP Clang..."
          AOSPTC_URL="https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/refs/heads/main/clang-r530567.tar.gz"
          aria2c -x 16 -s 16 -o "clang.tar.gz" "$AOSPTC_URL"
          tar -xf clang.tar.gz -C toolchain/llvm-arm-toolchain-ship/10.0
          rm clang.tar.gz

      - name: Verify Toolchain Integrity
        run: |
          echo "--- Kiểm tra trình liên kết (Linker) ---"
          LINKER_PATH="toolchain/gcc/linux-x86/aarch64/aarch64-linux-android-4.9/bin/aarch64-linux-android-ld"
          if [ -f "$LINKER_PATH" ]; then
            echo "✅ Thành công: Linker đã được tìm thấy tại $LINKER_PATH"
          else
            echo "❌ Thất bại: Không tìm thấy Linker. Cấu trúc thực tế là:"
            find toolchain/gcc -name "aarch64-linux-android-ld"
            exit 1
          fi

      - name: Build Kernel
        run: |
          chmod +x build_kernel.sh
          ./build_kernel.sh
        env:
          KBUILD_BUILD_USER: "Lixin"
          KBUILD_BUILD_HOST: "GitHub-Actions"

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Vanilla-Kernel-A73
          path: |
            out/arch/arm64/boot/Image
            out/arch/arm64/boot/dts/samsung/*.dtb
            out/arch/arm64/boot/dtbo.img

